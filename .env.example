# Supabase project backing the chat box. Both values are public — the anon
# key is designed to ship in client bundles. Copy to .env for local dev.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
